extends Resource
class_name CardPoolEntry

## A single entry in the card sprite pool.
## Contains a SpriteFrames resource and the frame index to use.

@export var sprite_frames: SpriteFrames
@export var frame_index: int = 0
